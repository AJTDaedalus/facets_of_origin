-r requirements.txt
pytest==8.3.2
pytest-asyncio==0.24.0
httpx==0.27.2

# Optional — front-end tests (tests/e2e/test_ui_flows.py) skip without it.
# Needs its browser too:  playwright install chromium
playwright==1.48.0
