# LOG — Production Apparatus (PHB + MM)

**Design:** `docs/DESIGN_production_apparatus.md`
**Tasks:** `docs/TASKS_production_apparatus.md`

---

## PA-1 — Doc-consistency test module — **done** (2026-07-12)

**Files:** `software/tests/test_docs_consistency.py` *(new, 2 tests)*.

Landed INV-5 and INV-6. Both pass against unmodified content; both were verified
red by probe (a temp file citing `Chapter IX.9`, and a `--` line appended to MM5)
before the probes were reverted. The module is now the safety net PA-2 renumbers
against.

**INV-5 — `test_cross_references_resolve`.** Walks every `Chapter ([IVX]+\.\d+[a-c]?)`
citation in `player_handbook/*.md` and `mm_manual/*.md` and maps it to a file by
filename prefix. All 28 current citations resolve (the DESIGN said 26 — the count
was low, not the claim; nothing dangles). Failure output is `file:line cites Chapter N`.

**INV-6 — `test_mm5_uses_typographic_dashes`.** Asserts no ASCII `--` in MM5 prose.

**Spec deviation, deliberate.** The task says MM5 "contains no ASCII `--`". Taken
literally that test cannot pass and never could: MM5 has 9 lines of legitimate
markdown structure — thematic breaks (`---`) and table delimiter rows (`|---|---|`) —
and D8 was never about those. The finding (review §D, item 8) was ASCII dashes in
*prose*, which render as literal double-hyphens. So INV-6 strips thematic breaks and
table delimiters, then scans what remains. This is the invariant D8 actually closed;
the literal reading would have been a permanently-red test.

**Suite:** 967 passed, 1 failed — the pre-existing, environment-caused
`test_config.py::test_default_port_is_8000` (`docs/TODO.md` T4). Baseline was 965/1,
so both new tests are accounted for and nothing else moved.

`playtest/02_silence_of_ashenmoor/digital_tool_log.md` was regenerated by the full
suite as expected, and reverted.

**Next:** PA-2 (Wave 1 — chapter renumber II.4 → II.4 + II.4a) is unblocked.

---

## PA-2 — Chapter renumber: II.4 → II.4 + II.4a — **done** (2026-07-12)

**Files:** `player_handbook/II.4_Character_Creation_Facets.md` (cut from),
`player_handbook/II.4a_Character_Creation_Facet_Body.md` *(new)*,
`player_handbook/Table_of_Contents.md`, `player_handbook/II.1_Character_Creation_Overview.md`,
`player_handbook/II.6_Character_Creation_Skills.md`, `software/facets/base/facet.yaml`
(header comment only).

**The cut.** Moved the two exact spans the DESIGN specified — `### Skills of the Body`
(lines 44–57) and `### Facet of the Body — Technique Tree` through the end of the Iron
branch (lines 103–204) — verbatim into the new `II.4a_Character_Creation_Facet_Body.md`.
`git diff` on II.4 confirms moves-only: the H1 retitle ("Facets" → "Facets and
Advancement") plus two deletions, no rewording. Heading levels in II.4a were left exactly
as they appeared in the source (H3 section headers, bold `**Tier N**` markers) rather than
promoted to match II.4b/c's H2/H3/H4 depth — that's a formatting-consistency call outside
"the cut" as scoped, and doing it risked failing the letter of the "moves only" acceptance
criterion for no real benefit (no invariant tests heading depth).

**Reference sweep.** Beyond the four `websocket.py`/`builder.js`/`test_websocket.py`
citations the DESIGN said to leave alone (verified: the "only skills used this session"
rule text they cite is still live in II.4, unchanged), found and retargeted three more
citations DESIGN §2.4 didn't enumerate but its sweep instruction covers:
- `facet.yaml:9` header comment — "match II.4 exactly" → "match II.4a exactly" (comment only, no data touched).
- `II.6_Character_Creation_Skills.md:9` — "Technique trees are in the Facet chapters (II.4, II.4b, II.4c)" → "(II.4a, II.4b, II.4c)". Left the adjacent "Full descriptions" false-pointer problem (II.6:69, II.6:85) alone — that's PA-3's job (DESIGN §2.2), not a renumbering fix.
- `II.1_Character_Creation_Overview.md:60` — the "Where to Go From Here" chapter list had no II.4a line at all, and its II.4 line claimed Technique trees live "within it," which is now false. Added an II.4a bullet matching the existing II.4b/c template ("Skills and Techniques for physical, forceful, and enduring characters") and corrected the II.4 bullet to describe what it now actually contains (skills/Facet levels/Techniques mechanics, not the Body tree). This is template reuse, not new prose — the b/c bullets already established the pattern.

**Verification:**
- `test_cross_references_resolve` (INV-5) passes — all citations resolve, including the new II.4a ones.
- Full suite: 967 passed, 1 failed (same known `test_config.py::test_default_port_is_8000`) — baseline held exactly.
- `playtest/02_silence_of_ashenmoor/digital_tool_log.md` regenerated by the suite run, reverted.
- `facet.yaml` data untouched (comment-only edit, confirmed by diff).

**Next:** Wave 2 unblocked — PA-3 (skill dedup), PA-4 (roll markers), PA-5 (character
sheet) can now proceed in any order; PA-6 (branch intros, Fable-only) is also unblocked
but out of Worker scope.

---

## PA-3 — Skill-text dedup — **done** (2026-07-12)

**Files:** `software/tests/test_docs_consistency.py` (new INV-1), `player_handbook/II.6_Character_Creation_Skills.md`,
`player_handbook/II.4a_Character_Creation_Facet_Body.md`, `II.4b_Character_Creation_Facet_Mind.md`,
`II.4c_Character_Creation_Facet_Soul.md`.

**TDD.** Wrote `test_skill_descriptions_match_facet_yaml` (INV-1) first: parses `facet.yaml`'s
15 skill `description` fields and II.6's `## The Skill List` prose entries, asserts (whitespace-normalized)
verbatim substring containment. Confirmed red against unmodified content — 3 Body entries had drifted
wording (Stealth/Finesse/Endurance each had inserted or reworded clauses that broke verbatim
containment) and all 10 Mind/Soul entries were unparseable one-line dashes, not prose paragraphs.

**Prose home: II.6 "The Skill List" is now canonical for all 15 skills**, uniform `**Skill**
*(Attribute)*` + paragraph format. Mind and Soul entries expanded from their existing II.4b/II.4c
"Covers" text (no new claims — reused wording, split into a base clause matching `facet.yaml`
verbatim plus a trailing usage sentence built from content that was already there). The 3 Body
entries with drifted wording were trimmed back to match `facet.yaml` verbatim at the clause level
(e.g. Stealth's "tailing someone without being noticed" → "tailing someone", moving the "unnoticed"
idea into the existing usage sentence) — compression, not rewording of the mechanic.

**Deleted the false pointers** (II.6:69, II.6:85 claiming full descriptions lived in II.4b/II.4c —
they never did, per DESIGN §2.2) and fixed the overview paragraph (II.6:9) that had the pointer
direction backwards.

**Facet chapters drop description columns.** II.4a/b/c skill tables now carry Skill + Attribute
only, each followed by "Full descriptions are in Chapter II.6 — Skills." `facet.yaml` untouched
(data home, per task rule 5).

**Complete Skill Reference table (II.6) left as-is** — checked it against the new prose; its
short-form wording differs in phrasing but doesn't contradict, which is what "compression" allows.

**Verification:** INV-1 red → green; full `test_docs_consistency.py` module 3/3 passed (INV-1, 5, 6);
INV-5 (cross-references) still passes — no chapter citations were touched.

**Next:** PA-4 (roll markers) or PA-5 (character sheet) — either is ready.

---

## PA-4 — Standardize roll markers — **done** (2026-07-12)

**Files:** `mm_manual/MM2_Session_Design.md`, `mm_manual/MM4_Running_the_Table.md`.

Grepped both files for `^> .*rolls \*\*2d6` — exactly the 5 lines the task predicted
(MM2:404, 424, 458; MM4:193, 203), no more, no fewer. Converted each `>` to `→`, leaving
the inner notation untouched (confirmed by `git diff`: every hunk is a single-character
marker swap, nothing else moved). Also grepped both files' full `>` line list to confirm
the advice-callout blockquotes (MM2:42, 70, 165, 496) and the player/MM dialogue examples
(MM2:203-214, which are quoted speech, not roll resolutions or advice — out of scope
either way) were untouched.

**Note, not fixed:** MM2:458 has `net -1` (ASCII hyphen); the PHB's equivalent phrasing
uses a typographic minus (`net −1`). DESIGN explicitly scopes PA-4 to the line marker only
("only the line marker changes") and INV-6 (typographic dashes) is scoped to MM5 alone —
so this is a real inconsistency but out of this task's bounds. Flagging for whoever owns
D8/INV-6 scope-widening, not fixing here.

**Verification:** `grep -n "^> .*rolls \*\*2d6"` on both files returns nothing.
`test_docs_consistency.py` full module still 3/3 (INV-6 doesn't cover MM2/MM4, so it
neither caught nor needed to catch this change).

**Next:** PA-5 (character sheet appendix) is ready.

---

## PA-5 — Character sheet appendix — **done** (2026-07-12)

**Files:** `player_handbook/Appendix_Character_Sheet.md` *(new)*, `player_handbook/Table_of_Contents.md`,
`software/tests/test_docs_consistency.py` (new INV-2).

**The sheet.** Six sections, transcribed from II.1:9-23's own table, in order: Attributes (9
Minor Attributes grouped under Body/Mind/Soul), Facet (Primary Facet, Facet Level, Advancement
Track), Background (Title & Origin, Starting Skill, Secondary Skill, Specialty), Skills (all 15,
rank + marks), Techniques (unlocked + choice made), Session Resources (Sparks, skill points
remaining). Fillable markdown tables throughout.

**Deliberate omission, flagged not fixed.** II.1's table names no "Character Name" / "Player
Name" field, and PA-5's rule is explicit — "no field II.1 does not name." A sheet with no name
line is unusual for a printable form, but adding one would be inventing a field this task's spec
forbids inventing. Leaving it out and flagging it here rather than quietly adding it, per the
project's "report, don't invent" pattern used elsewhere in this wave (e.g. PA-9's glossary rule).
Worth a BRIEF-level call on whether II.1's own description should grow a 14th named item, not a
Worker-level fix.

**TDD.** Wrote `test_character_sheet_fields_map_to_model` (INV-2) with a hardcoded
`CHARACTER_SHEET_FIELDS` dict — 13 entries, one per named item in II.1's six sections — mapping
each human-readable label to a `Character` model attribute (declared field or `@computed_field`).
Checks both directions: every mapped attribute must exist on `Character`
(`model_fields | model_computed_fields`), and every label must appear (whitespace-normalized) in
the sheet file, so the dict can't drift from either side. Multiple labels share one attribute
where that's genuinely where the data lives — Starting Skill, Secondary Skill, and the Skills
table all point at `skills`, since the model has no separate "starting skill" field, just the one
dict. Probed both failure modes by hand (bad attribute name; label removed from the mapping) to
confirm the test actually catches them before trusting the green run — same probe discipline
PA-1 used on INV-5/6.

**Verification:** INV-2 passes; full `test_docs_consistency.py` module 4/4; full suite 969
passed, 1 failed (same known `test_config.py::test_default_port_is_8000`) — baseline (967) + 2
new tests (INV-1, INV-2) accounted for exactly. ToC lists "Appendix B: Character Sheet".
`playtest/02_silence_of_ashenmoor/digital_tool_log.md` regenerated by the full-suite run and
reverted.

**Wave 2 status:** All three Worker-scope tasks (PA-3, PA-4, PA-5) done. Wave 2 as a whole
is not complete — PA-6 and PA-7 are Fable-only and untouched, PA-8 is still blocked on the
Brain Q1 ruling — so Wave 3 (blocked on *all* of Wave 2 per the TASKS header) is not yet
unblocked. Nothing left in Worker scope until PA-6/7/8 land or Wave 3 gets a scope
exception; escalating that question rather than guessing at it.

---

## Brain rulings + PA-6, PA-7, PA-8 — **done** (2026-07-12, Fable)

**Rulings (full rationale in `docs/DECISIONS.md`):**
- **B1 (Q1):** III.2 grows, does not fold. Resolves P17; PA-8 executed same pass.
- **B2 (Q2):** digital-first confirmed — no print milestone exists anywhere in the repo, and
  digital-first is the project's founding constraint. PA-10 is the right artifact and is now
  unblocked (behind PA-9 only).
- **B3 (PA-5 escalation):** the sheet gets a Character Name / Player Name header. The defect was
  in II.1's description, not the sheet — the engine requires both fields (`character.py:69-70`).
  Canonical text amended first (II.1 gains one sentence), then the sheet header block, then
  INV-2's mapping (`Character Name → name`, `Player Name → player_name`).

**PA-6 — branch intros.** Six intros written to the Clarity template (em-dash construction,
names the attribute's character, states what the branch is for): Might, Grace, Iron in II.4a;
Presence, Fortune, Communion in II.4c. Each claim is grounded in that branch's actual Techniques
(e.g. Fortune's "unlocked door, guard called away" is Convenient Coincidence's own text;
Communion's "thin places" is Sense the Unseen's). No mechanics implied that the tiers don't grant.

**PA-7 — front matter.** `player_handbook/Front_Matter.md` (new): how-to-read paths (novice →
Quick Start; player → I, II.1–II.6, III.1, reference the rest; MM → everything + MM1–5; Facet
modules optional), GPLv3 license statement with repo URL, credits (contributors via repo; TTRPG
Safety Toolkit — Kienna Shaw and Lauren Bryant-Monk, matching MM4's existing credit; no
SRD-derived material currently exists in either book, so none is claimed). Novice on-ramp also
landed in I_Introduction: an explicit Quick Start pointer in "How to Use This Handbook" and a
three-sentence "whole shape of it" passage for first-time players (the voice call DESIGN §4.7
left to Fable — taken, kept minimal). ToC gains a Front Matter line.

**PA-8 — III.2 grown.** Two In Play vignettes, recurring cast, continuing the established
Thornwall/Millhaven arc: "The Mill at Millhaven" (Threat Clock demo — advance on partials/
failures, roll-free wind-back shown via Zulnut, fill applies Winded) and "The Beam" (the
scar-or-heroic-death choice — MM states both options faithfully, Mordai takes the scar, player
and MM name it together). No rules text altered; the vignettes demonstrate only what the chapter
already states. **New canon:** Mordai's shoulder is permanently scarred; recorded in
`references/phb-examples.md` with the II.3/III.3 arc summaries so future vignettes stay
consistent. **Owner may veto the scar** — it is the one durable consequence this pass creates.

**Also fixed:** MM2:458 `net -1` → `net −1` (the PA-4 flag; matches II.2:182's typographic minus).

**Verification:** full suite 969 passed / 1 failed — identical to the PA-5 baseline (the 1 is the
known pre-existing `test_config.py` T4 failure). INV-2 green with the two new name fields; INV-5
green across the new Front Matter and III.2 citations. Playtest digital_tool_log reverted.

**Wave 2 is complete. PA-9 (glossary) is unblocked and is the next Worker task.** PA-10 follows
it (Q2 ruled); PA-11 last.

---

## PA-9 — Glossary — **done** (2026-07-12)

**Files:** `player_handbook/Glossary.md` *(new)*, `player_handbook/Table_of_Contents.md`,
`software/tests/test_docs_consistency.py` (new INV-3).

**54 entries**, alphabetized, sourced from the seed list in the TASKS spec plus the actual
canonical text (no new terms invented beyond the seed). Each entry: `**Term** — one/two-sentence
definition. *(pointer)*`. Read the full source chapter for every term before writing its entry —
II.2, II.3, II.4, II.4a/b/c, II.5, II.6, III.1, III.2, III.3, IV.1, MM1, and the Mirror Master
passage in I_Introduction — to guarantee every definition is a compression of existing text, per
the quick-ref law and this workstream's "report, don't invent" rule.

**Two seed-list corrections, not inventions:**
- The seed list writes "Scope (Minor/Standard/Major)"; the actual canonical tiers (II.3) are
  **Minor/Significant/Major** — there is no "Standard" scope tier, that's a different table
  (Domain Type × Scope → Difficulty). Used the real terms.
- "Focused/Standard/Prismatic domain" became **Domain Type (Focused/Standard/Broad-Prismatic)**
  as the bolded term, matching II.3's own heading ("Domain Types") and its actual three-way
  name for the third type ("Broad (Prismatic)").

**No term required "report, don't define."** Every seed-list term had a canonical home; nothing
was flagged as undefined.

**Pointer format — a deliberate departure from the DESIGN spec's literal `*(Chapter X.Y)*`.**
Two entries cite MM1 (Threat Rating, Encounter Budget), which isn't `X.Y`-shaped, and the books
themselves never write "Chapter MM1" (see `Front_Matter.md`, `Table_of_Contents.md` — always bare
"MM1"). Followed the established convention: `*(Chapter X.Y)*` for PHB chapters (including bare
`*(Chapter I)*`, also already attested in `Front_Matter.md:13`), bare `*(MM1)*` for MM-manual-only
terms. This is a citation-format detail, not a rules call — flagging it here rather than treating
it as silently obvious, since it's the one place this task didn't follow the DESIGN doc literally.

**Ascendant Domain and Second Domain** are defined identically in both II.4b (Mind) and II.4c
(Soul) — each Facet's tree carries its own copy of the same Technique text with a different
domain-list reference. Cited II.4b for Ascendant Domain and II.4c for Second Domain arbitrarily;
either chapter would satisfy INV-3 since both contain the term.

**TDD.** Wrote `test_glossary_pointers_resolve` (INV-3) before the glossary existed — confirmed
red (`FileNotFoundError` on `Glossary.md`). It reuses `_chapter_numbers()` from INV-5 rather than
inventing a second resolver, since that helper already maps every filename prefix (`I`, `II.4b`,
`MM1`, …) to its file. Probed both failure modes by hand before trusting the green run: a bad
chapter token (`II.99`) and a term absent from its cited chapter's text — both caught with a
correctly-attributed error message, then reverted.

**Verification:** INV-3 passes; full `test_docs_consistency.py` module 5/5; full suite **970
passed, 1 failed** (same known `test_config.py::test_default_port_is_8000`, T4) — baseline (969)
+ 1 new test (INV-3), exactly accounted for. ToC gains a "Glossary" line under Appendices.
`playtest/02_silence_of_ashenmoor/digital_tool_log.md` regenerated by the full-suite run and
reverted via `git checkout --`.

**Next:** PA-10 (generated index) is ready — Q2 was ruled (B2, digital-first confirmed) and PA-9
is now landed, so both of PA-10's blockers are clear.
